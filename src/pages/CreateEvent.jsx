function CreateEvent() {
  // we should check the local storage for the presence of a token
  // if this does not exist, redirect the user to the homepage
  return <div>CreateEvent Page</div>;
}

export default CreateEvent;
